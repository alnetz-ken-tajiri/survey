/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["hucupssurvey.vercel.app", "v0.blob.com", "images.unsplash.com", "hucups-survey.s3.ap-northeast-1.amazonaws.com"], // "images.unsplash.com" を追加
    },
  };
  
  export default nextConfig;
  